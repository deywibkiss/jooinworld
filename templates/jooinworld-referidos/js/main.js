/**** Event Handler for { Template }****/( function( $, window, document ){		var TemplateHandler = function( a ){				// atributes, selector or global vars		this.msg_oculta = '#system-message';	};		TemplateHandler.prototype ={						/**			 * Inializes the functions when DOM ready			 */				initialize: function(){								this.ocultaMsg();							}						/**			* Inicializo configuracion de facebook			*/		,	ocultaMsg: function(){						$( this.msg_oculta ).delay( 15000 ).fadeOut( 1000 );		}									};		// Use this when the script is loaded in <head> tag	$( document ).ready( function(){		// Expose to global scope		window.TemplateHandler = new TemplateHandler();		window.TemplateHandler.initialize();	});	})( jQuery, this, this.document , 'undefined' );