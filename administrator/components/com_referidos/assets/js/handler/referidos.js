/**** Event Handler for { Referidos API }****/( function( $, window, document, controller, Utilities ){		var ReferidosHandler = function( a ){				// atributes, selector or global vars		this.buttonadd = '#addcat';		this.linkDelete = '.elimina-caract';		this.modalcomision = '.modalcomision';			};		ReferidosHandler.prototype ={						/**			 * Inializes the functions when DOM ready			 */				initialize: function(){								this.addCaracteristica();				this.deleteCaract();				this.showModalComi();							}			/**			 * Function header			 *			 * @param {}			 * @return {}			 */		,	addCaracteristica: function(){				$( this.buttonadd ).click( function( e ){										e.preventDefault;										var data = Utilities.formToJson( '.form-createplan' );										data.nomcaract = $( '#caracteristicas option:selected' ).data( 'nomcaract' );										console.log( data );										controller.addCaracteristica( data );									});			}		/**		* eliminar caracteristica agregada al plan		*/			, deleteCaract : function(){						$( 'body' ).delegate( this.linkDelete ,  'click' , function(e){				console.log( $( this ).parents( 'tr' ) );				var dataops = $( this ).data( 'idops' );				$( this ).parents( 'tr' ).remove();				$( '#ops-'+dataops).attr('disabled', false);			});		}		/**		 * mostrar modal de comisiones de usuario		 *		 * @param {}		 * @return {}		 */		, showModalComi: function(){							var _this = this;					$( this.modalcomision ).click( function( e ){								e.preventDefault();								var iduser = $( this ).data( 'iduser' );				console.log( iduser);								controller.showModalComi( iduser );			});		}						};		// Use this when the script is loaded in <head> tag	$( document ).ready( function(){		// Expose to global scope		window.ReferidosHandler = new ReferidosHandler();		window.ReferidosHandler.initialize();	});	})( jQuery, this, this.document, this.ReferidosController, this.Misc, undefined );