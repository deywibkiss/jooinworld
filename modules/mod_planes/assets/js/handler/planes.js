/**** Event Handler for { Planes }****/( function( $, window, document ){		var PlanesHandler = function( a ){				// atributes, selector or global vars		this.importContFb = '.import-contacts-facebook';	};		PlanesHandler.prototype ={							/**				 * Inializes the functions when DOM ready				 */					initialize: function(){										this.initSlider();									}												/**				* Set Slider				*				*/			,	initSlider: function(){										// setear el ancho de la lista a partir de la suma de los li					var ancho = 0;					var witdthLi = 0;					var count = 0;										$( '.list_planes > li' ).each( function( key, value ){												witdthLi = $( value ).outerWidth(true);						ancho += $( value ).outerWidth(true);						count++;						//console.log( ancho );					});										ancho += witdthLi;										$( '.list_planes' ).width( ancho );										var numScroll = 0;										// definir el evento de la flecha izquierda					$( '.flecha-prev' ).on( 'click mousedown', function( e ){						e.preventDefault();						if( numScroll >= 0)							$( '#planes-list' ).scrollLeft( numScroll-=witdthLi );					});										// definir el evento de la flecha derecha					$( '.flecha-next' ).on( 'click mousedown' , function( e ){						e.preventDefault();						//console.log( witdthLi , numScroll);						if( numScroll < witdthLi )							$( '#planes-list' ).scrollLeft( numScroll+=witdthLi );					});				}							};		// Use this when the script is loaded in <head> tag	$( document ).ready( function(){		// Expose to global scope		window.PlanesHandler = new PlanesHandler();		window.PlanesHandler.initialize();	});	})( jQuery, this, this.document, 'undefined' );